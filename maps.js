let num = [1, 2, 3, 4, 5];

const incrementedNum = num.map(number => number + 1); // Create a new array with incremented values

console.log(incrementedNum); // Output: [2, 3, 4, 5, 6]

//the only diffrence between foreach and maps is that maps creates a new array