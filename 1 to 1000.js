function sumDigits1() { //the main function of the program
    let sum = 0;  //inisalizes the sum variable 
    for (let i = 1; i <= 1000; i++) { //initalizes the i variable stores numbers from 1 to 1000 and stores it in sum 
      sum += i; //adds the inital value of i to the value stored in sum
    }
    return sum;
  }